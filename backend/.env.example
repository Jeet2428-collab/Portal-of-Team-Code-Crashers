# ==============================================================================
# CODE CRASHERS (TEAM CC) - REAL-TIME EMAIL & SMTP CONFIGURATION
# ==============================================================================

# Database
DB_NAME=cc
DB_USER=root
DB_PASSWORD=system2026
DB_HOST=localhost
DB_PORT=3306

# Email / SMTP Configuration
# For Gmail:
# 1. Enable 2-Step Verification on your Google Account
# 2. Generate an "App Password" under Google Account Security -> App Passwords
# 3. Paste the 16-character App Password below
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_16_char_app_password
DEFAULT_FROM_EMAIL=Code Crashers <your_email@gmail.com>
